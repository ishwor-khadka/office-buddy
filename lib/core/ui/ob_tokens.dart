import 'package:flutter/material.dart';

class ObTokens {
  // Brand colors pulled from the logo (mint + cool blue/purple).
  static const Color mint = Color(0xFFADEBB3);
  static const Color mintDeep = Color(0xFF39B983);
  static const Color sky = Color(0xFF6DB8FF);
  static const Color iris = Color(0xFF7C6BFF);

  // Surfaces (glass works best on very light, tinted bases).
  static const Color canvas = Color(0xFFF7FBF8);
  static const Color canvasAlt = Color(0xFFF1F7FF);
  static const Color text = Color(0xFF0B1B14);
  static const Color textMuted = Color(0xFF3C5A4B);

  static const Radius r16 = Radius.circular(16);
  static const Radius r24 = Radius.circular(24);
  static const Radius r32 = Radius.circular(32);
}

